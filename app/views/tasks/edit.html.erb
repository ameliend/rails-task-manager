<%= simple_form_for(@task) do |f| %>
  <%= f.input :title %>
  <%= f.input :details %>
  <%= f.input :completed %>
  <%= f.submit %>
<% end %>

<%= link_to "destroy", delete_path(@task), method: :delete, class:"btn btn-danger" %>
